#include <iostream>
#include <cmath>
#include <algorithm>
#include <vector>

#define int long long

using namespace std;

const int SIZE = 2*pow(10, 5) + 2;
vector<int> board(SIZE, 0);

struct point {
    int a, b, c, d;

    

}

void solve() {

    int n;
    int a, b, c, d;

    cin >> n;
    
    while(n--) {

        cin >> a >> b >> c >> d;


    }

}

int32_t main() {

    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    freopen("ADWINS.INP", "r", stdin);
    freopen("ADWINS.OUT", "w", stdout);

    int t;
    cin >> t;
    
    while (t--) {   
        solve();
    }
    
}