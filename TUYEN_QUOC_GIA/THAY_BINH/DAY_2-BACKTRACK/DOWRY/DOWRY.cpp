#include <iostream>
#include <vector>

#define ull unsigned long long
#define ll long long

using namespace std;

ll 

int main() {

    freopen("DOWRY.INP", "r", stdin);
    freopen("DOWRY.OUT", "w", stdout);
    
    int n;
    ll l, r;
    cin >> n >> l >> r;
    vector<ll> w(n), v(n);
    for(int i = 0; i < n; i ++) {
        cin >> w[i] >> v[i];
    }

    

    return 0;
}